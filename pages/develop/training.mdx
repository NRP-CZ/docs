# Invenio NRP training

# Introduction


# Installation

* nrp devtools branch rdm-12 curl
```
curl -sSL https://raw.githubusercontent.com/oarepo/nrp-devtools/rdm-12/nrp-installer.sh >nrp-installer.sh
```

```
bash nrp-installer.sh my-repo
```

TODO: verze do metadat

cd my-repo

./nrp check-requirements ???

./nrp develop

vytvoreni uzivatele jako ownera genericke komunity
+ mozna nejakeho normalniho uzivatele

todo: genericka komunita ve fixtures

TODO: anyone user can make a community on /communities

workflows

# Configuration

ui konfigurace - how to change logo, colors

# Novy model

1. nrp model create

yaml

2. nrp model compile

?? smazat databazi?

2. ./nrp develop

uprava modelu - pridat keywords

models/{model_name}-metadata.yaml
keywords[]:
    type: fulltext


nrp model compile

./nrp reindex || drop

./nrp develop a nechat bezet

cmdline client + vytvoreni zaznamu + API

./nrp ui detail model_name

vyzkouset ze se zobrazi detail pro zaznam z api

search ui - pridat keywords
do ResultsListItemu {model_name}/semantic-ui/js/{model_name}/search/ResultsListItem.jsx
Vytahnout novou hodnotu z vysledku vyhledavani

const keywords = _get(result, "metadata.keywords", []);

zobrazit v odpovidajici lokace
 {keywords.length > 0 && (
    <Grid.Row className="ui separated">
    <span
        aria-label={i18next.t("Keywords")}
        title={i18next.t("Keywords")}
    >
        {_join(
        keywords.map((keyword) => keyword),
        ", "
        )}
    </span>
    </Grid.Row>
)}



do deposit formu pridat keywords

a. do formulare ({model_name}/semantic-ui/js/{model_name}/forms/FormFieldsContainer.jsx)
    import {StringArrayField} from "@js/oarepo_ui/forms"
b. pridat na miste kde se input ma zobrazit
   <StringArrayField
          fieldPath="metadata.keywords"
          addButtonLabel={i18next.t("Add keyword")}
          {...getFieldData({ fieldPath: "metadata.keywords" })}
    />

vyzkouseni "hotoveho" repozitare

preklady ???
./nrp make translations

ccmm